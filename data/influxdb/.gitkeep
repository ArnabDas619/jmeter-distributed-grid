# This file ensures the data/influxdb directory is tracked by git
# The actual InfluxDB data files are ignored via .gitignore
# 
# InfluxDB stores its time-series data here, which persists across container restarts
